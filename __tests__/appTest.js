'use strict';

describe('Initiate Jest test suite is running', function(){
	it('expect true to be true', function(){
		expect(true).toBe(true);
	});

	it('expect one plus two is three', function(){
		expect(1+2).toBe(3);
	});
});
